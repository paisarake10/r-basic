# para instalar utilizamos el paquete como arg entre comillas
install.packages("tidyverse", dep = TRUE)
# para cargar solo utilizamos el nombre del paquete ya descargado sin comillas
library(tidyverse)

install.packages("magic", dep = TRUE)

library(magic)

# Crea un cuadrado magico donde las filas, las columnas y las diagonales suman lo mismo
magic(6)

installed.packages()

# se pueden hacer operaciones por consola separadas por punto y coma inline
2*2; 5+1; 10*10

# se utiliza como decimal el punto y no la comma. pues la coma se usa para separar coordenadas de un vector ig.